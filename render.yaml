services:
  - type: web
    name: nova
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python botforge_boot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.6
